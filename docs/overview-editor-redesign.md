# 写文章 / 编辑文章 页面重设计

> 参考 Medium · Notion · GitHub 编辑器 · Vercel Dashboard —— 以「专注写作」为核心的现代博客编辑器。

## 改了什么

### 布局（一、二、三、十七）
- 顶部留白大幅压缩，进入页面第一眼就是编辑器
- 标题为无边框大字号输入框（24px / 700），占位「请输入文章标题...」
- 正文为无边框大字号写作区（16px / 行高 1.8 / 最小 600px），输入时自动增高
- 写作卡片 `:focus-within` 时整体浮现品牌色光环
- Card 最大宽度约 896px（`max-w-4xl`），内容留白收敛

### 发布设置（四、五、六）
- 分类 / Slug / 标签 从正文上方下沉到「发布设置」区，编辑区保持纯净
- 标签从复选框改为 **Tag Chip**：点击切换，Hover / Active（品牌色填充）状态
- 所有表单控件（Select / Slug Input）统一 `field-control` 圆角 / 边框 / Focus Ring

### 交互（七、十、十一、十二、十五）
- 保存按钮区分 Hover / Active(`scale` 回弹) / Disabled / Loading（「正在保存...」+ spinner）
- 新增「存为草稿」次要按钮
- **自动保存**：停止输入 1.5s 后静默存草稿，顶部状态点变绿并显示「已保存 · 时:分」
- **Ctrl / Cmd + S** 快捷保存
- 离开页面若有未保存内容，浏览器弹出确认
- 底部实时显示「X 字 · 约 Y 分钟阅读」
- Toast 轻提示（已保存草稿），动画 150–250ms

### 命名统一（八、九）
- 全站「笔记」→「文章」；Hero「CREATE NOTE」→「写文章」
- 导航「写笔记」→「写文章」，并增加当前页 Active 状态（写文章/编辑文章高亮）

### 后端支持
- `notes` 表新增 `status`（草稿/已发布）与 `slug`（SEO 友好，自动去重生成）
- 新增 `POST /notes/autosave` 自动保存端点（返回 JSON，首次建草稿、后续更新，不把已发布文章降级为草稿）
- Dashboard 显示草稿徽标

## 怎么用
1. 浏览器强制刷新 `http://localhost:9090/notes`（需登录）
2. 直接写标题 → 写正文 → 底部选分类/标签/Slug → 点「发布文章」
3. 输入过程中会自动存草稿；也可 `Ctrl/Cmd+S` 或点「存为草稿」

## 注意
- 自动保存依赖 `notes.autosave`，已置于 `auth` 中间件保护
- 草稿也会被写入 `notes` 表（status=draft），Dashboard 以「草稿」徽标区分
- Slug 为中文标题时由 `Str::slug` 回退处理，建议手动填写英文 Slug 以获得最佳 URL
