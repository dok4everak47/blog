# 迁移约定: 迁移文件建好后不要改名

## Laravel 迁移约定

### 规则
- **迁移文件创建后不要改名**。Laravel 用文件名（如 2026_07_10_073445_create_tags_table）做迁移去重依据，改名 = 新身份 = 会重复执行。
- 要调整表结构时，追加新的迁移文件（ALTER TABLE），不要改旧文件。

### 事故记录 (2026-08-01)
- 现象: migrate 报 SQLSTATE[42P07] Duplicate table: relation "tags" already exists
- 根因: 数据库 migrations 表有孤儿记录 batch 1 = 2026_07_31_135519_create_tags_table（文件已删除/改名，git 查无此文件），它建的 tags 表缺 name 列。当前迁移文件是 2026_07_10_073445_create_tags_table.php（带 name 列），文件名对不上 → 被当新迁移重跑 → 撞表。
- 修复: DROP TABLE tags (0 行数据) + DELETE FROM migrations WHERE migration='2026_07_31_135519_create_tags_table' → php artisan migrate 全过。

### 排查流程
1. php artisan migrate:status 找 Pending 但表已存在的迁移
2. SELECT * FROM migrations; 找孤儿记录
3. 对比数据库实际表结构和迁移文件内容
4. 清理孤儿记录/残缺表后重跑

